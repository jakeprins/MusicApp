<h1><%= @artist.name %></h1>

<%= image_tag @artist.image.url if @artist.image.present? %>
<%= link_to "Add new song", new_song_path( artist_id: @artist.id ), class: "btn btn-default", style: "float: right" %></h2>


<h3>Songs</h3>

<p><% songs = @artist.songs.all %></p>

<p><%= render songs, hide_artist: true %></p>


<!-- <% @artist.songs.each do |song| %>
<%= link_to song.title, song_path(song.id) %>
<% end %> -->
